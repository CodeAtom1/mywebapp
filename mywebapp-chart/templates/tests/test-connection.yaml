apiVersion: v1
kind: Pod
metadata:
  name: "mywebapp-test-connection"
  labels:
    test: connection
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ .Release.Name }}-svc:{{ .Values.ingress.port }}']
  restartPolicy: Never
